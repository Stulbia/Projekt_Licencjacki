{% extends 'base.html.twig' %}
{% block title %}Zmień okładkę{% endblock %}

{% block body %}
    <h1>Zmień okładkę dla "{{ book.title }}"</h1>
    {{ form_start(form) }}
    {{ form_row(form.cover) }}
    <button class="btn">Zapisz</button>
    {{ form_end(form) }}
{% endblock %}
