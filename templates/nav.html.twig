<nav class="navBar">
    <div class="navLogo">
        <a href="{{ path('homepage') }}">
            <span class="material-symbols-outlined">menu_book</span> Biblioteka
        </a>
    </div>

    <button id="hamburger"
            class="navHamburger"
            aria-label="Menu"
            aria-controls="navLinks"
            aria-expanded="false">
        <span class="ms-icon material-symbols-outlined" aria-hidden="true">menu</span>
    </button>


    <div id="navLinks" class="navLinks" data-open="false">

    {# === DROPDOWN MOTYWU === #}
    <div class="themeDropdownWrapper">
        <button class="navLink themeToggleBtn"
                id="themeToggleBtn"
                aria-haspopup="true"
                aria-expanded="false"
                aria-controls="themeDropdown">
            <span class="material-symbols-outlined" aria-hidden="true">palette</span>
            Motyw
            <span class="material-symbols-outlined themeChevron" aria-hidden="true">expand_more</span>
        </button>

        <ul class="themeDropdown" id="themeDropdown" role="menu" hidden>
            <li role="none">
                <button class="themeOption" data-theme-option="light" role="menuitemradio" aria-checked="false">
                    <span class="themeSwatch" style="background:linear-gradient(135deg,#f9f3eb 50%,darkorange 50%, #00977f 80%  )"></span>
                    Jasny
                </button>
            </li>
            <li role="none">
                <button class="themeOption" data-theme-option="dark" role="menuitemradio" aria-checked="false">
                    <span class="themeSwatch" style="background:linear-gradient(135deg,#01011a 50%,#00d9f7 50%, deeppink 90%)"></span>
                    Ciemny
                </button>
            </li>
            <li role="none">
                <button class="themeOption" data-theme-option="forest" role="menuitemradio" aria-checked="false">
                    <span class="themeSwatch" style="background:linear-gradient(135deg,  #efefed 50%, #6d8935 50%, #00643a 80%)"></span>
                    Leśny
                </button>
            </li>
            <li role="none">
                <button class="themeOption" data-theme-option="contrast" role="menuitemradio" aria-checked="false">
                    <span class="themeSwatch" style="background:linear-gradient(135deg,#000 50%,#ffff00 50%)"></span>
                    Kontrast
                </button>
            </li>
        </ul>
    </div>



        <a class="navLink" href="{{ path('homepage') }}">Strona Głowna</a>
        <a class="navLink" href="{{ path('for_you_index') }}">Polecane</a>
        <a class="navLink" href="{{ path('book_search') }}">Szukaj</a>

        {% if app.user %}
            <a class="navLink" href="{{ path('library_index') }}">Biblioteczka</a>
            <a class="navLink" href="{{ path('review_index') }}">Twoje Recenzje</a>
            <a class="navLink" href="{{ path('user_index') }}">Twój profil</a>
            <a class="navLink loginBtn" href="{{ path('app_logout') }}">Wyloguj</a>
        {% else %}
            <a class="navLink" href="{{ path('app_login') }}">Zaloguj</a>
            <a class="navLink loginBtn" href="{{ path('user_register') }}">Zarejestruj</a>
        {% endif %}
    </div>

</nav>